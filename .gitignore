# Dependencies
node_modules/

# Next.js
.next/
out/

# Environment (secrets)
.env
.env.local
.env.production
*.pem
backend/.env
backend/copernicus_key.pem

# Python
__pycache__/
*.pyc
.venv/

# Build artifacts
tsconfig.tsbuildinfo
next-env.d.ts

# IDE
.idea/
.vscode/
*.swp

# pnpm store (Docker volume artifact)
.pnpm-store/
.claude/worktrees/

# Landing build tools (local only, not deployed)
landing/tailwindcss.exe

# Output of scripts/regenerate-report.sh — PDFs copied out of the backend
# container for admin inspection. Lead-identifying, never commit.
regenerated-reports/


# Lighthouse raw audits — large, regenerate on demand (see docs/seo-baselines/README.md)
docs/seo-baselines/lighthouse-*.json
